# Perfil de desarrollo — habilita el endpoint de demo login (JWT sin validar
# contraseña vía /api/auth/login-demo). NUNCA activar este perfil en producción
# (no debe estar en spring.profiles.active por defecto ni en la variable de
# entorno SPRING_PROFILES_ACTIVE de Render). Ver application.properties.
demo.auth.enabled=true
